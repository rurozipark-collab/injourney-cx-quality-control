# InJourney Airports CX Quality Control
# Slim requirements focused on Daily Service QC (Harian) + professional exports.
# Heavy RAG/AI stack (langchain, chromadb, etc.) removed because AI features were disabled.
# This makes build + cold start MUCH faster on Streamlit Community Cloud (free tier).

# Core Web Framework
streamlit>=1.32.0
streamlit-extras>=0.4.0

# PDF Export (Daily Service QC reports - rapih & modern)
reportlab>=4.2.0

# Excel Export (with photo support)
openpyxl>=3.1.0
pandas>=2.2.0
xlsxwriter>=3.2.0

# Image handling (for embedding foto bukti in PDF/Excel)
Pillow>=10.3.0

# Visualization (Dashboard & metrics)
plotly>=5.22.0

# Utilities
python-dotenv>=1.0.0
python-dateutil>=2.9.0

# Note: 
# - No langchain / chromadb / sentence-transformers / openai anymore → faster startup.
# - Knowledge Base tab will show "not available" message (AI features were removed per request).
# - Daily Service QC, exports, photo evidence, and data persistence (JSON) remain fully functional.
# - First cold start on free tier can still take 15-40s after idle. Subsequent loads are fast.
